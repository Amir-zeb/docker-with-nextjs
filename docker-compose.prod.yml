services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: docker-with-nextjs-prod
    ports:
      - "8080:3000"
    env_file:
      - .env.production
    restart: always